# WechatShort 短剧项目 · 服务器部署指引

> **本文档的读者是在目标服务器上执行部署的 AI 助手（Claude Code）。**
> 小程序端已在开发机调试完毕，本次任务只做**服务器侧**：后端服务 + 管理后台 + 数据库 + Redis + Nginx。
>
> **小程序本身不部署到服务器**——它由开发者在 HBuilderX 编译、微信开发者工具上传提审。服务器只提供它调用的 HTTPS 接口。

---

## 0. 架构（先看懂再动手）

```
微信小程序（用户手机）
      │  HTTPS 443
      ▼
┌─────────────────────────────────────────────┐
│ Nginx        api.你的域名.com                │
│  ├─ /           → /usr/local/pages/duanju-admin  （Vue2 静态后台）
│  └─ /sqx_fast/  → 127.0.0.1:8183                 （Spring Boot 后端）
└─────────────────────────────────────────────┘
                        │
              ┌─────────┴─────────┐
              ▼                   ▼
      MySQL 127.0.0.1:3306   Redis 127.0.0.1:6379
      库 duanju_yizhao        database=2

视频/图片 → 阿里云 OSS（不落服务器本地磁盘）
```

**关键约束：管理后台和后端接口必须同域。** `duanju-admin/src/utils/httpRequest.js:48` 写死了相对路径 `'/sqx_fast/' + actionName`，不走任何 baseUrl 配置。所以后台静态页和 `/sqx_fast/` 反代必须挂在同一个 nginx server 块下，不能分域名部署。

**技术栈事实**（已核对源码，不要凭猜测改）：

| 项 | 值 | 出处 |
|---|---|---|
| 后端框架 | Spring Boot 2.6.11 | `duanju-server/pom.xml:14` |
| JDK | **1.8（必须）** | `pom.xml:20` `<java.version>1.8</java.version>` |
| 端口 | 8183 | `application.yml` `server.port` |
| context-path | `/sqx_fast` | `application.yml` `server.servlet.context-path` |
| 生产 profile | `prod` → `application-prod.yml` | |
| jar 名 | `duanju-server.jar` | `pom.xml:425` `<finalName>${project.artifactId}</finalName>` |
| 数据库脚本 | `duanju-server/db/duanju_yizhao.sql`（72 张表） | |
| 后台框架 | Vue 2.5 + webpack **3.6** + node-sass **4.14.1** | `duanju-admin/package.json` |
| 后台构建 Node | **Node 14（必须，见 §7 陷阱）** | |

---

## 1. 开工前必须向用户索取的信息（**不许自己编造、不许填假值**）

源码交付时所有密钥都被替换成了 `__占位符__` 形式，仓库里**不含任何真实凭证**。下列信息只有用户手上有，缺哪一项就停下来问，不要用示例值糊过去：

| # | 需要的信息 | 缺了会怎样 | 能否先跳过 |
|---|---|---|---|
| 1 | **已 ICP 备案的域名** + SSL 证书（fullchain.pem / privkey.pem） | 微信小程序拒绝调用未备案域名、拒绝 http、拒绝 IP | ❌ 硬门槛 |
| 2 | 微信小程序 **AppID / AppSecret** | 小程序登录、支付、播放器全部 401 | ❌ |
| 3 | MySQL 可建库账号密码 + 要给业务库设的密码 | 起不来 | ❌ |
| 4 | Redis 密码 | 不设可留空，但公网机器建议设 | ✅ 可留空 |
| 5 | 阿里云 OSS AccessKeyId / Secret / bucket / region | 后台上传剧集视频、封面失败 | ✅ 先跳过，后台其余功能可用 |
| 6 | 微信支付 mchId / apiKey | 充值下单失败 | ✅ 先跳过 |
| 7 | 抖音 / B站 小程序凭证 | 只做微信端就不需要 | ✅ 整段删除 |

`wx.miniapp.configs` 里的 `token` 和 `aesKey` 是**你自己定义**的消息推送校验串（要和微信后台"消息推送"配置里填的一致），不是微信发的，可以生成随机串；`__JWT_SECRET__` 同理，随机串即可。

**完整占位符清单在 `duanju-server/配置说明-必填项.md`，动手前先读一遍。**

---

## 2. 装依赖

```bash
apt update
apt install -y openjdk-8-jdk mysql-server redis-server nginx
java -version    # 必须是 1.8.0_xxx
```

⚠️ **JDK 必须是 8**。项目锁 `java.version=1.8`，依赖里的 Shiro 1.4 / Druid 老版本在 JDK 11+ 上会撞反射封装报 `InaccessibleObjectException`。装了多版本就用 `update-alternatives --config java` 切到 8。

Maven **不需要装在服务器上**——见 §5，jar 由开发机构建后 scp 过来。如果用户坚持在服务器上构建，`apt install -y maven`（需 3.6+）。

---

## 3. 初始化 MySQL

```bash
# 建库（必须 utf8mb4，剧名/简介有 emoji）
mysql -uroot -p -e "CREATE DATABASE duanju_yizhao CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;"

# 建业务账号（不要让应用用 root）
mysql -uroot -p -e "CREATE USER 'duanju'@'localhost' IDENTIFIED BY '用户给的密码';
GRANT ALL PRIVILEGES ON duanju_yizhao.* TO 'duanju'@'localhost'; FLUSH PRIVILEGES;"

# 导入表结构 + 基础字典数据
mysql -uduanju -p duanju_yizhao < duanju-server/db/duanju_yizhao.sql
```

**验收**：

```bash
mysql -uduanju -p duanju_yizhao -e "SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='duanju_yizhao';"
# 期望 72
```

### 3.1 sql 文件里的占位符

`db/duanju_yizhao.sql` 第 1745 行左右含 `your-admin-domain.com`（种子配置里的域名），导入后修掉：

```bash
mysql -uduanju -p duanju_yizhao -e \
  "UPDATE common_info SET value=REPLACE(value,'your-admin-domain.com','api.你的域名.com') WHERE value LIKE '%your-admin-domain.com%';"
```

### 3.2 `common_info` 表里的运行时配置

导入后这张表有若干 `value` 仍是 `__占位符__`，需要按用户提供的凭证填（也可登录后台在界面里填）：

- `__WX_APPID__` / `__WX_SECRET__` / `__WX_TOKEN__` / `__WX_AESKEY__`：微信小程序（后台侧读取）
- `__OSS_ACCESS_KEY_ID__` / `__OSS_ACCESS_KEY_SECRET__`：阿里云 OSS
- `__WXPAY_API_KEY__` / `__WXPAY_MCH_ID__`：微信支付
- `__SMS_ACCESS_KEY_ID__` / `__SMS_ACCESS_SECRET__`：阿里云短信
- `__UMENG_PUSH_*__`：友盟推送（App 端才用，小程序可不填）

查还有哪些没填：

```bash
mysql -uduanju -p duanju_yizhao -e \
  "SELECT id,name,value FROM common_info WHERE value LIKE '\_\_%\_\_';"
```

---

## 4. 初始化 Redis

编辑 `/etc/redis/redis.conf`：

```conf
bind 127.0.0.1
requirepass 用户给的Redis密码     # 不设密码就整行注释掉，同时 application-prod.yml 里删掉 password 行
```

```bash
systemctl enable --now redis-server
redis-cli -a '密码' ping    # 期望 PONG
```

后端用的是 **database 2**（`application-prod.yml` 写死），别改到 0 去。

---

## 5. 填后端配置 + 构建

### 5.1 `duanju-server/src/main/resources/application-prod.yml`

整个文件就 11 行，全是占位符，全部要填：

| 占位符 | 填什么 |
|---|---|
| `__RDS_MYSQL_HOST__` | `127.0.0.1`（本机 MySQL） |
| `__DB_NAME__` | `duanju_yizhao` |
| `__DB_USERNAME_2__` | `duanju` |
| `__DB_PASSWORD_4__` | MySQL 业务账号密码 |
| `__REDIS_HOST__` | `127.0.0.1` |
| `__DB_PASSWORD_5__` | Redis 密码（无密码则删掉整行 `password:`） |

### 5.2 `application.yml`

只填 `wx.miniapp.configs` 里 **name=`app1`** 那一条（小程序端 `TENANT='app1'` 对应的就是它，两边必须一致）：

```yaml
      - name: app1
        appid: 真实AppID
        secret: 真实AppSecret
        token: 自定义随机串
        aesKey: 自定义随机串
        appKey:                  # 虚拟支付 appKey，没有就留空
        msgDataFormat: JSON
```

其余的 `app1gzh`（公众号）、`app2`（第二个小程序租户）、`douyin.miniapp.configs` 下几十条抖音应用、B站应用——**用户只做微信单端就把这些整段删掉**，留着占位符启动时会解析成非法凭证，日志刷错误。

底部 `__JWT_SECRET__` 填随机串。

### 5.3 源码里硬编码的次要占位符

只影响对应功能，不阻塞启动，按需处理：

- `common/utils/QRCodeUtil.java:166` → `http://YOUR_SERVER_IP:8088`（二维码里的落地地址）
- `common/constant/OceanEngineConstant.java` → 巨量引擎投放，不投流可不管
- `modules/douyin/.../DouyinThirdPartyServiceImpl.java`、`modules/place/...` → 抖音/第三方平台对接，不用可不管
- `pom.xml:46` `<remote-addr>YOUR_SERVER_IP:22</remote-addr>` → wagon 自动部署用，本方案不用它

### 5.4 构建

**推荐在开发机构建，服务器只收 jar**（服务器不用装 maven，也避免首次拉依赖几百 MB）：

```bash
# 开发机
cd duanju-server
mvn clean package -Dmaven.test.skip=true
ls -lh target/duanju-server.jar
scp target/duanju-server.jar root@服务器IP:/opt/duanju/
```

在服务器上构建则是同样两条命令，产物在 `target/duanju-server.jar`。

⚠️ 仓库自带的 `deploy.sh` / `build.sh` / `start.sh` **不要直接执行**：
- `deploy.sh` 里 scp 目标是占位符 `YOUR_SERVER_IP`
- `build.sh` 推的是占位符阿里云镜像仓库 `your-registry.cn-region...`
- `start.sh` 会 `git pull`，且用 `ps aux | grep duanju-server` 取 PID 后 kill——匹配范围过宽，可能误杀其他进程

---

## 6. 后端做成 systemd 服务

写 `/etc/systemd/system/duanju.service`：

```ini
[Unit]
Description=duanju-server (WechatShort backend)
After=network.target mysql.service redis-server.service

[Service]
Type=simple
WorkingDirectory=/opt/duanju
ExecStart=/usr/bin/java -Xms1g -Xmx1g -Djava.security.egd=file:/dev/./urandom \
  -jar /opt/duanju/duanju-server.jar --spring.profiles.active=prod
SuccessExitStatus=143
Restart=always
RestartSec=5
StandardOutput=append:/var/log/duanju/stdout.log
StandardError=append:/var/log/duanju/stderr.log

[Install]
WantedBy=multi-user.target
```

```bash
mkdir -p /opt/duanju /var/log/duanju
systemctl daemon-reload
systemctl enable --now duanju
journalctl -u duanju -f          # 盯启动日志
```

**验收**：

```bash
ss -lntp | grep 8183                      # 端口在听
curl -i http://127.0.0.1:8183/sqx_fast/   # 有 HTTP 响应（404/302 都算通，说明容器起来了）
```

内存：`-Xmx1g` 适配 2C4G 机器。如果服务器是 2G 内存，降到 `-Xms512m -Xmx768m`。注意 `Dockerfile` 里写的是 `-Xms2g -Xmx2g`，别照抄到小机器上。

---

## 7. 构建管理后台（**这一步最容易翻车，先读完再动手**）

### 7.1 Node 版本陷阱 ⚠️

`duanju-admin` 是 2018 年的 vue-cli 模板：**webpack 3.6.0 + node-sass 4.14.1**。

- Node ≥ 17：webpack 3 用 md4 哈希，OpenSSL 3 移除了它 → 构建直接崩 `ERR_OSSL_EVP_UNSUPPORTED / error:0308010C:digital envelope routines::unsupported`
- Node ≥ 16：node-sass 4.14.1 没有对应的预编译 binding，源码编译也失败

**必须用 Node 14 构建**：

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 14 && nvm use 14
node -v          # 期望 v14.x
```

`npm i` 卡在 node-sass 下载时换镜像：

```bash
npm i --sass-binary-site=https://npmmirror.com/mirrors/node-sass
```

### 7.2 替换后台源码里的域名占位符

4 个 `.vue` 文件把上传接口写死成了 `https://your-admin-domain.com/...`，不改会上传失败：

```bash
cd duanju-admin
grep -rl "your-admin-domain.com" src/
# src/views/sysmission/missionRedact.vue     ← xhr.open POST /sqx_fast/alioss/upload
# src/views/sysmission/missionAdd.vue        ← 同上
# src/views/user/userDetail.vue              ← 默认头像 /sqx_fast/logo.png
# src/views/user/userDetail1.vue             ← 同上

grep -rl "your-admin-domain.com" src/ | xargs sed -i 's#https://your-admin-domain\.com#https://api.你的域名.com#g'
```

顺手把 `static/config/index-prod.js` 里的 `window.SITE_CONFIG['baseUrl'] = 'https://YOUR_SERVER_IP/sqx_fast'` 也改成真实域名。（注：这个 baseUrl 实际已被 `httpRequest.js` 的相对路径旁路掉，只是留着别扎眼；`domain` 和 `version` 两行**不要动**，是构建脚本要替换的锚点。）

### 7.3 构建

```bash
npm run build        # 走 gulpfile.js（内部调 node build/build.js，再做版本号目录 + cdnUrl 替换）
```

产物结构（gulp 会把 `static` 挪进带时间戳的版本号目录，再删掉原 `dist/static`）：

```
dist/
├── index.html                    ← 生产分支只加载 ./config/index.js
├── config/index.js               ← index-prod.js + init.js 拼接而成，负责动态加载下面的资源
└── 2607251230/static/{js,css,img,plugins}/
```

**如果 gulp 链报错**（gulpfile 里 `gulp.series('clean', gulp.parallel(...), 'build', ...)` 的依赖写得有重复触发问题，`build` 可能被并发跑多次而互相删文件），改用这条等效的手工路径：

```bash
rm -rf dist
node build/build.js                              # 出标准 dist/{index.html,static/}
mkdir -p dist/config
cat static/config/index-prod.js static/config/init.js > dist/config/index.js
```

这样 `cdnUrl = './' + '' = './'`，`init.js` 会去加载 `./static/js/app.js`，与 `node build/build.js` 的产物结构正好对上，能正常跑，只是少了版本号目录带来的缓存刷新能力。

### 7.4 部署静态文件

```bash
mkdir -p /usr/local/pages/duanju-admin
# 从构建机传：
scp -r dist/* root@服务器IP:/usr/local/pages/duanju-admin/
```

**验收**：`/usr/local/pages/duanju-admin/index.html` 和 `config/index.js` 都存在，且 `config/index.js` 里 `baseUrl` 不含 `YOUR_SERVER_IP`。

---

## 8. Nginx

`/etc/nginx/sites-available/duanju.conf`：

```nginx
server {
    listen 80;
    server_name api.你的域名.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.你的域名.com;

    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 500m;      # 后台要上传剧集视频
    proxy_read_timeout   300s;      # 大文件上传别被掐断

    gzip on;
    gzip_types text/css application/javascript application/json;
    gzip_min_length 1k;

    # 管理后台（Vue 静态）
    location / {
        root /usr/local/pages/duanju-admin;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端接口：URI 原样透传，后端 context-path 本身就是 /sqx_fast
    location /sqx_fast/ {
        proxy_pass http://127.0.0.1:8183;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Swagger 文档不对公网开放
    location /sqx_fast/swagger { deny all; }
    location /sqx_fast/swagger-ui.html { deny all; }
}
```

注意 `proxy_pass http://127.0.0.1:8183;` **末尾不带路径**——这样 `/sqx_fast/xxx` 会原样转给后端，正好命中 context-path。写成 `proxy_pass http://127.0.0.1:8183/;`（带斜杠）会把 `/sqx_fast` 前缀吃掉，全部 404。

```bash
ln -s /etc/nginx/sites-available/duanju.conf /etc/nginx/sites-enabled/
nginx -t && systemctl reload nginx
```

---

## 9. 验收清单（逐条跑，全绿才算完）

```bash
# 1. 三个服务都在
systemctl is-active mysql redis-server nginx duanju

# 2. 后端本地通
curl -i http://127.0.0.1:8183/sqx_fast/

# 3. 接口经 nginx 通（走真实域名，不要用 IP —— 要顺带验证证书）
curl -i https://api.你的域名.com/sqx_fast/

# 4. 后台首页能出
curl -s https://api.你的域名.com/ | grep -q "短剧运营管理系统" && echo OK

# 5. 静态资源没 404（在浏览器 F12 Network 里确认 app.js / vendor.js / app.css 全 200）

# 6. 证书链完整（微信对证书链不完整的域名会拒绝请求）
echo | openssl s_client -connect api.你的域名.com:443 -servername api.你的域名.com 2>/dev/null | grep -E "Verify return code"
# 期望 Verify return code: 0 (ok)

# 7. 8183 没暴露公网（在服务器之外的机器上跑）
curl --max-time 5 http://api.你的域名.com:8183/    # 期望超时/拒绝

# 8. 数据库里没剩占位符
mysql -uduanju -p duanju_yizhao -e "SELECT COUNT(*) FROM common_info WHERE value LIKE '\_\_%\_\_';"
```

浏览器打开 `https://api.你的域名.com/`，用 **`admin` / `admin888`** 登录，**登录成功后第一件事就是改密码**（这是交付文档里的公开默认口令）。

---

## 10. 安全收口（必做，别跳）

```bash
ufw allow 22 && ufw allow 80 && ufw allow 443 && ufw enable
ufw status                     # 确认 8183 / 3306 / 6379 都不在放行列表里
```

- MySQL：确认 `bind-address = 127.0.0.1`（Ubuntu 默认就是）
- Redis：确认 `bind 127.0.0.1` 且设了 `requirepass`
- 后台默认口令 `admin888` 改掉
- Swagger 已在 nginx 层 deny（见 §8）
- **如果用户要求走 Docker 部署**：`duanju-server/Dockerfile` 的 ENTRYPOINT 里挂着
  `-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=8017`——这是远程调试端口，**能直接接管 JVM，生产镜像必须删掉这段**。同时 base image 写的是占位符私有仓库 `your-registry.cn-region.personal.cr.aliyuncs.com/...`，换成文件里第一行注释掉的 `openjdk:8-jdk-alpine`。

---

## 11. 服务器侧不负责、但要提醒用户的收尾项

部署完成后把这几条回报给用户，**这些不要自己动手改**：

1. **微信公众平台 → 开发管理 → 服务器域名 → `request 合法域名`** 加入 `https://api.你的域名.com`。不加，小程序正式版所有请求被拦。
2. **小程序端替换后端域名**：`uniapp-duanju/common/config.js` 的三个 `your-api-domain.com`，以及另外 **30 多个 `.vue` / `.js` 文件**里硬编码的同一域名，需要全局替换后重新编译上传。这一步在开发机的 HBuilderX 侧做。
3. **微信官方短剧播放器插件**（项目用的 provider `wx94a6522b1d640c3b`）要求：企业主体 + 《网络文化经营许可证》 + 微短剧备案，再申请插件权限。本地调试能过是因为开发版跳校验，正式版没权限会白屏。这是比服务器部署更长的前置项。

---

## 12. 排障对照表

| 现象 | 原因 | 处置 |
|---|---|---|
| 启动即退，日志 `Communications link failure` | `application-prod.yml` 的 MySQL 主机/账号/密码没填对 | 核对 §5.1 |
| 启动即退，日志 `NOAUTH Authentication required` | Redis 设了密码但配置里没填（或反之） | 核对 §4 |
| 启动即退，`InaccessibleObjectException` | JDK 不是 8 | `update-alternatives --config java` |
| 后台页面白屏，Network 里 js/css 全 404 | `dist/config/index.js` 缺失，或版本号目录和 cdnUrl 不匹配 | 用 §7.3 的手工路径重建 |
| 后台能开但所有接口 404 | nginx `proxy_pass` 末尾多写了 `/` | 去掉斜杠，见 §8 |
| 后台接口 200 但返回登录页 | Shiro token 失效 / Redis database 不是 2 | 检查 `application-prod.yml` 的 `database: 2` |
| 上传视频 413 | `client_max_body_size` 太小 | 见 §8 |
| 上传视频报 OSS 错 | `common_info` 里 OSS AccessKey 还是占位符 | 见 §3.2 |
| 小程序真机请求失败、开发者工具却正常 | 域名没进微信白名单 / 证书链不完整 / 未备案 | 见 §11.1、§9.6 |
| `npm i` 或 `npm run build` 报 `ERR_OSSL_EVP_UNSUPPORTED` | Node 版本太高 | 切 Node 14，见 §7.1 |

---

## 13. 明确禁止的动作

1. **不要修改 `uniapp-duanju/` 目录下任何文件。** 小程序端由开发者在 HBuilderX 侧维护，服务器上的改动不会进包，只会造成两边不一致。
2. **不要把 `uniapp-duanju/uniCloud-aliyun_bak` 改回 `uniCloud-aliyun`。** 这是刻意改名的：小程序端不用 uniCloud，HBuilderX 认到该目录会报"未关联 uniCloud 服务空间"。内容原样保留，做 App 端时才需要改回。
3. **不要直接执行 `start.sh` / `deploy.sh` / `build.sh`**（占位符 IP、会 git pull、kill 匹配过宽），用本文档 §5、§6 的方式。
4. **不要用 JDK 11/17 构建或运行后端。**
5. **不要用 Node 16/18/20 构建管理后台。**
6. **不要把 8183 / 3306 / 6379 暴露到公网。**
7. **不要把填好真实密钥的配置文件 commit 进 git。** 仓库里的 `__占位符__` 是交付方刻意留的，提交真实值等于把凭证公开。要持久化就用环境变量或 `--spring.config.additional-location=` 指向服务器上 git 忽略的外部配置文件。
8. **改不通的地方不要自行绕过或编造凭证**——停下来把缺的信息列给用户。
