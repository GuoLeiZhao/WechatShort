# 微信官方短剧播放器接入 · 现状与待办

> 记录时间：2026-07-26
> 项目：`/root/agent工作空间/WechatShort`
> 小程序 AppID：`wxe54afd8d50e75b06`，播放器插件 provider：`wx94a6522b1d640c3b`
> 本文所有结论均已对照代码验证，标注了 `文件:行号`，可直接复查。

---

## 一、当前进度

| 环节 | 状态 |
|---|---|
| 小程序编译 / 真机预览 | ✅ 通 |
| 官方播放器插件能拉起 | ✅ 通（真机；开发者工具会报 30001） |
| 插件内能看到剧 | ✅ 用 `dramaId=100056` 测试时能看到 |
| **点开能播放** | ❌ **卡在这里** |
| 后台配置真实短剧 | ⬜ 未开始 |

---

## 二、播放链路（已验证）

### 正常入口（应该走这条）

```
剧场首页 pages/index/index.vue:552
  → uni.navigateTo('/me/detail/detailMPWechat?id=<本地courseId>')
  → detailMPWechat.vue:44   注册 playletPlugin.onPageLoad(this._onPlayerLoad)
  → detailMPWechat.vue:102  PlayerManager.navigateToPlayer({dramaId: 本地courseId})
  → playerManager.js:16     GET /app/course/selectCourseDetailsById?id=<本地courseId>
  → playerManager.js:23     用返回的 course.wxMediaId 拼 plugin-private:// 跳插件
  → 插件回调 onPageLoad → playerManager.js:325 getEncryptData(dramaId)
  → GET /app/wechatDrama/getUserCanPlayDramaSerialNo/{wxMediaId}
  → playerManager.js:334    pm.setCanPlaySerialList(...)  ← 插件靠这个才知道哪集能播
```

**关键**：传给插件页的 `dramaId` 是 `course.wx_media_id`（微信媒资剧目ID），
不是本地 `course_id`。后端 `selectCourseDetailsById` 负责这一步转换。

### 测试入口（已于 2026-07-26 关闭）

`pages/me/index.vue:42` 的「我的钱包」标题，`v-if` 已改回 `false`。
它 `uni.navigateTo` **直接跳插件页**（`pages/me/index.vue:194`），
绕开了唯一注册 `onPageLoad` 的 `detailMPWechat.vue:44`。

---

## 三、"点开不播"的根因（两道闸，都堵着）

### 闸 1：`onPageLoad` 回调没注册

播放器插件自己不决定哪集能播，靠宿主小程序在 `onPageLoad` 回调里调
`pm.setCanPlaySerialList(...)` 告知。走测试入口时该回调从未注册 → 插件手里
没有任何一集是「可播」→ 能看见、点不动。

- 本项目注册位置：`me/detail/detailMPWechat.vue:44`（**页面级**）
- 参考 demo 注册位置：`官方播放器用法参考/app.js:139`（**App onLaunch，全局**）

demo 的做法更稳，从任何入口进都有授权。

### 闸 2：`if (that.encryptData)` 无兜底

`common/playerManager.js:329`：

```js
this.getEncryptData(info.dramaId).then(res => {
    that.encryptData = res?.data?.encryptedData;
    if (that.encryptData) {          // ← 拿不到就整段跳过，静默失败
        pm.setCanPlaySerialList({...})
    }
})
```

`getEncryptData`（`playerManager.js:614`）请求
`/app/wechatDrama/getUserCanPlayDramaSerialNo/{dramaId}`，需要：

- `course` 表里有 `wx_media_id` = 该 dramaId 的记录
- 本地有登录态 `userId` + `sessionKey`

任一缺失 → `encryptData` 为空 → 不调 `setCanPlaySerialList` → 一集都不给播，**且不报错**。

对比：demo 在拿不到后台配置时有兜底（默认前 7 集免费，`官方播放器用法参考/app.js:170`），所以 demo 能播。

---

## 四、dramaId 的两级，及后台能力边界

| 层级 | 字段 | 来源 | 后台能填吗 |
|---|---|---|---|
| 剧级 | `course.wx_media_id` | 微信媒资剧目ID | ❌ 后台无此字段 |
| 集级 | `course_details.wx_media_id` | 微信媒资文件ID | ✅ 可由 `initWxCourseMedia` 自动灌 |

### 后台「资源中心」= `duanju-admin/src/views/mission/mission.vue`

菜单 `sys_menu` menu_id=44「资源中心」，url 指向 `mission`。文件名叫 mission，
但管的是短剧，调 `course/*` 和 `courseDetails/*` 全套接口。

**能做**：建剧 / 改剧 / 假删除、上下架、推荐位、批量加集（`insertCourseDetailsMulti`）、
批量删集、批量改集价格和积分、分类管理、封面详情图上传（走 alioss）。

**不能做**：填 `wx_media_id`。
全库检索 `grep -rni "wxmediaid|wx_media|媒资ID|dramaId" duanju-admin/src/` **零命中**。

原因：这套后台原本为「自有播放器 + m3u8/OSS 视频」设计（表单里有"上传视频""视频链接"），
微信官方播放器是后加的能力，后端做了，后台 UI 没跟上。

### 微信媒资接口的现有用法

| 接口 | 封装位置 | 现有调用 |
|---|---|---|
| `/wxa/sec/vod/listmedia` | `WechatMpServiceImpl.java:80` | 仅 `CourseServiceImpl.java:416`，**永远传 dramaId** |
| `/wxa/sec/vod/getmedialink` | `WechatMpServiceImpl.java:90` | 仅 `CourseServiceImpl.java:382` |

**重要**：`ListMediaRequest.java` 里 `dramaId` 字段注释明确写「非必填」，
返回体 `ListMediaResponse.MediaInfo` 带 `mediaId` / `dramaId` / `name` /
`auditDetail.status`（1审核中 2驳回 3通过 4驳回重填）。

→ **不传 dramaId 调 listmedia 就能拉出账号下全部媒资，自动发现剧目ID和审核状态。**
现有代码从没这么用过（已核实：全库仅一处 `new ListMediaRequest`，且必传 dramaId；
git 无删除记录，无其他分支）。这是**十几行就能补上的能力**，不是缺功能。

---

## 五、已确认的缺陷清单

按对主流程的影响排序。

### A. 会直接毁数据 — `updateWxMediaIdByCourseIdAndName` 的 CASE 无 ELSE

`duanju-server/src/main/resources/mapper/course/CourseDetailsDao.xml:66`

```sql
UPDATE course_details
SET wx_media_id = CASE course_details_name
    WHEN #{item.name} THEN #{item.mediaId}   -- foreach 展开
END                                           -- ← 没有 ELSE
WHERE course_id = #{courseId}                 -- ← 作用于该剧全部集
```

SQL 中 CASE 无匹配且无 ELSE 返回 **NULL**。配合 `initWxCourseMedia` 的分页
（每页 100 条）：第 1 页写入 1–100 集后，第 2 页处理 101–150 集时，
**1–100 集因不在第二批 CASE 列表里被整批清成 NULL**。

→ **超过 100 集的剧，同步完只有最后一页有 media_id。** 100 集以内不受影响。

修法：`END` 前加 `ELSE wx_media_id`。

### B. 未判空 NPE — `getCanPlayDramaSerialNo`

`duanju-server/.../wechat/service/impl/WechatDramaPlayerServiceImpl.java:50`

```java
Course course = courseDao.selectCourseInfoByWxMediaId(dramaId);
Long courseId = course.getCourseId();   // ← 没判空
```

媒资ID 在 `course` 表查不到对应剧 → NPE → 前端静默不播，无任何提示。
同一方法里 `Integer.parseInt(courseCount)` 在无集数据时同样会崩。

### C. `initWxCourseMedia` 是一次性初始化，不是增量同步

`CourseServiceImpl.java:411`，选剧 SQL `CourseDao.xml:325`：

```sql
where wx_media_out_date is null and wx_media_id is not null
```

跑完把 `wx_media_out_date` 置为当前时间 → **同一部剧不会被处理第二次**。
后续在媒资加了新集，再调此接口不会有任何反应。
重跑必须手动 `UPDATE course SET wx_media_out_date = NULL WHERE course_id = ?`。

另外两点：
- 分页页数按**本地** `course_details` 行数算（`courseCount`），不是按微信实际集数。本地建少了，微信那边多出的集拉不到。
- 是 UPDATE 不是 INSERT，本地无对应集号的行 → 该条媒资静默丢弃。**必须先建齐集记录**。

### D. 集名解析硬编码

`CourseServiceImpl.java:422`（`initWxCourseMedia`）和 `:383`（`refreshWechatVideoUrl`）：

```java
String mediaName = mediaInfo.getName().split(" - ")[1];
String name = mediaName.replace("第","").replace("集","").replace(".mp4","");
```

要求微信媒资里每个文件名严格为 **`剧名 - 第N集`**（注释示例：`我的演艺 - 第1集`）。
命名不符 → `split(" - ")[1]` 数组越界 → 整个同步崩。

### E. `refreshWechatVideoUrl` 的定时器被注释

`CourseServiceImpl.java:405` 的 `@Scheduled(fixedRate = 6000 * 10)` 是注释状态。
mp4 临时链接 2 小时过期后无人刷新。
**仅影响自有播放器 `me/detail/detail.vue`；官方播放器插件不读 mp4Url。**

### F. `synCourse`（后台「同步资源」按钮）

按钮：`mission.vue:52` → `tongbu()` `mission.vue:2091` → GET `course/synCourse`
→ `CourseServiceImpl.java:220`。
按钮显隐由 `common_info` type=247「是否开启同步资源」控制（`mission.vue:2078`）。

**它同步的是第三方采集站的 m3u8 视频源，与微信媒资无关，全程不碰 `wx_media_id`。**
数据源是 `common_info` type=250「短剧视频采集地址」，本仓库 seed 值为占位符
`__VIDEO_COLLECT_API__`（未替换则 `HttpClientUtil.doGet` 直接抛异常）。

问题：
- **永远提示「操作成功」**：异步 `singleThreadPool.submit` + 立即 `return Result.success()`，
  异常被 catch 只写日志 `log.error("同步视频出错：")`，无法回传前端。
  验证是否真同步：查日志搜「同步视频出错」，或对比 `select count(*) from course`。
- `for (int i = 2; ...)` **无条件丢弃返回数组前两条**。
- 判重 `selectCount(eq("course_details_name", name))` **未带 course_id**，
  不同剧的同名集会互相误判并覆盖（`selectOne(...limit 1)`）。
- `/course/synCourse` 在 `ShiroConfig.java:44` 配成 **anon**，无需登录即可调用。

**结论：走微信官方播放器不需要这个按钮，不要用。**

---

## 六、下一步操作清单

> 2026-07-26 更新：原步骤 0–3（手抄剧目ID → 后台建剧建集 → 手填 `wx_media_id` →
> 调 `initWxCourseMedia`）已被后台新按钮「一键同步微信短剧」整体替代，见下。

### 步骤 1 · 点后台「一键同步微信短剧」

「资源中心」页顶部按钮 → 确认 → 弹窗给出同步报告（每部剧的剧目ID / 本地剧ID / 剧名 /
新建还是已存在 / 微信集数 / 新增·更新·无变化集数 / 过审·审核中·未过审集数），
解析不了的文件名列在弹窗下方。

同步结果自查两点：

- **「已过审」为 0 的剧播不了**，得先去微信公众平台把剧集提审通过。
- **「解析不出集号」的文件要改名**，格式按 `剧名 - 第N集`。

### 步骤 2 · 上架 + 定价

同步进来的新剧默认 **status=2（下架）+ is_price=2（免费）**，
封面、简介、分类都是空的。在「资源中心」列表里补齐后手动上架、按需改价。

### 步骤 3 · 验证

从**剧场首页**点剧封面进（不是已关闭的测试入口）。

### ⚠️ 不要再调 `initWxCourseMedia`

`GET /sqx_fast/app/course/initWxCourseMedia` 走的是缺陷 A 的坏 SQL
（CASE 无 ELSE，超 100 集的剧会被清空 media_id），且新按钮同步进来的剧
`wx_media_out_date` 是 NULL，正好落在它的选剧条件里，一调就中招。
该接口未修复前不要用，新按钮已完全覆盖它的功能。

---

## 七、待确认信息

- [ ] 微信媒资里实际有哪些剧？剧目ID、集数、集名格式、审核状态？
- [ ] `dramaId=100056` 到底对应哪部剧、是不是本账号的？（该值是 2026-07-25 替换占位符
      `__DEMO_MEDIA_ID__` 时随手填的，非查证所得；但 2026-07-26 真机测试确认能拉到剧集信息）
- [ ] 后端服务在 `wefly.work` 上是否已跑通、数据库是否已建？
      （判断方法：手机上剧场首页能否正常显示剧列表）
- [ ] `common_info` type=250 的值是否仍是占位符？

---

## 八、本次会话的改动

| 时间 | 改动 | 位置 |
|---|---|---|
| 2026-07-26 | 测试入口 `v-if="true"` → `v-if="false"`，关闭「我的钱包」测试入口 | `uniapp-duanju/pages/me/index.vue:42` |
| 2026-07-26 | 新增后台「一键同步微信短剧」按钮（下详） | 见下表 |

### 「一键同步微信短剧」实现

| 文件 | 改动 |
|---|---|
| `CourseService.java` | 加接口 `Result syncWechatDrama()` |
| `CourseServiceImpl.java` | 实现 + 三个私有方法 `listAllWxMedia` / `buildCourseFromMedia` / `parseDramaTitle` / `parseEpisodeNo` |
| `CourseController.java` | `GET /course/syncWechatDrama`（走 shiro 鉴权，非 anon） |
| `mission.vue` | 顶部按钮 + `syncWechatDrama()` 方法 + 同步结果弹窗 |

**它做什么**：不传 `dramaId` 调 `/wxa/sec/vod/listmedia` 分页拉全量媒资（每页 100，上限 50 页）
→ 按 `dramaId` 分组 → 每组查 `course.wx_media_id`，没有就建一部新剧（默认下架+免费）
→ 按集号比对 `course_details`，缺的插入、`wx_media_id` 不一致的更新 → 返回同步报告。

**刻意绕开的老坑**：

- 不走 `updateWxMediaIdByCourseIdAndName`（缺陷 A 的 CASE 无 ELSE），改成逐条 insert/updateById
- 不走 `CourseDetailsService.insert`（里面带抖音上传），改成 `courseDetailsDao.insert`
- 更新集时只 set `courseDetailsId` + `wxMediaId`，其余字段留 null 靠 MP 的 NOT_NULL 策略跳过，
  不会覆盖后台已配好的价格封面
- 不依赖 `wx_media_out_date`，可反复重跑，幂等
- 同步执行、返回真实报告，不学 `synCourse` 的「异步提交 + 永远提示操作成功」
- 集号解析先去扩展名再取**第一组**数字，否则 `第12集.mp4` 会命中 `mp4` 里的 `4`

**没做的事**：闸 1（`onPageLoad` 只在页面级注册）和闸 2（`encryptData` 无兜底）没动，
缺陷 A/B/C/D/E/F 也都没改。这个按钮只解决「数据灌不进去」，不解决「灌进去了还是不播」。

以上改动**本机未编译验证**（无 JDK/Maven）。Vue 侧已做 JS 语法检查和模板标签闭合检查，均通过。

`test()` 方法（`pages/me/index.vue:193`）和 `me/wallet/wallet.vue:114` 里带
`dramaId=100056` 的跳转代码仍保留，因入口隐藏而不可达。

---

## 九、纠错记录（防止下次重蹈）

本次会话中曾给出后被推翻的判断，记录以免再犯：

1. ~~「`dramaId=100056` 是假 id，媒资库里没有」~~
   → 真机上能拉到剧集信息。开发者工具报 `ret 30001 系统异常` 是
   **devtools 环境**导致（日志含 `client_platform: "devtools"`、`is_logged_in: false`），
   官方播放器插件在工具端取播放地址本就常失败，与 id 有效性无关。

2. ~~「duanju-admin 没有短剧管理页面」~~
   → 有，就是「资源中心」`src/views/mission/mission.vue`。文件名叫 mission 容易误判，
   必须进文件看 `adornUrl` 调的接口，不能只看目录名。

3. ~~「剧级 dramaId 后端拿不到，只能手填」~~
   → `ListMediaRequest.dramaId` 非必填，不传即可拉全量媒资并发现剧目ID。
   现有代码没这么用，但能力存在。

**教训**：判断「有没有某功能」时，目录名和文件名不可信，必须落到接口调用和字段名上检索。
