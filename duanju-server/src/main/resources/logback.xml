<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG.FORMAT" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId}] %-5level %logger{50} [%L] %.-1024msg%n"/>
    <property name="LOG.PATH" value="${LOG_PATH:-/var/log/duanju-server}"/>
    <property name="LOG.LEVEL" value="${LOG_SQL_LEVEL:-info}"/>

    <!--引入默认的一些设置-->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <!--web信息-->
    <logger name="org.springframework" level="info"/>
    <logger name="com.sqx" level="info"/>
    <logger name="org.springframework.scheduling" level="info"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG.FORMAT}</pattern>
        </encoder>
    </appender>

    <!-- 异常日志 -->
    <appender name="EXCEPTION_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG.PATH}/exception.log</file>
        <append>true</append>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <!--<receiver>your-alert-email@example.com</receiver>-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG.PATH}/exception.log.%d{yyyy-MM-dd}_%i</fileNamePattern>
            <maxFileSize>500MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG.FORMAT} %caller{0}</pattern>
        </encoder>
    </appender>

    <!-- 服务执行日志 -->
    <appender name="ROOT_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG.PATH}/root.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG.PATH}/root.log.%d{yyyy-MM-dd}</fileNamePattern>
            <maxHistory>1</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${LOG.FORMAT}</pattern>
        </encoder>
    </appender>

    <!--开发环境:打印控制台-->
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ROOT_APPENDER"/>
    </root>
    <root level="error">
        <appender-ref ref="EXCEPTION_APPENDER"/>
    </root>

</configuration>
